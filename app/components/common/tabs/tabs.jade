.tabs
  ul.tabs__list
    li(ng-repeat='tab in $ctrl.tabs')
      a(
        href='#',
        ng-bind='tab.label',
        ng-class="{'tabs__active-tab': tab.selected}"
        ng-click='$ctrl.selectTab($index);'
      )
  .tabs__content(ng-transclude)